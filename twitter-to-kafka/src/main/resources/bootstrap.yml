spring:
  application:
    name: twitter-to-kafka-service
  profiles:
    active: twitter_to_kafka
  cloud:
    config:
      # config-server-repository file name
      name: twitter-to-kafka-service, config-client
      uri: http://localhost:8888
      username: admin
      password: admin
